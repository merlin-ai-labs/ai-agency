# OpenAI (set via environment variable or GitHub Secrets in CI)
OPENAI_API_KEY=your-openai-api-key-here

# OpenWeather (needs to be set)
OPENWEATHER_API_KEY=your-key-here

# LLM Provider
LLM_PROVIDER=openai

# Database (Cloud SQL Proxy on port 5433)
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5433/ai_agency
