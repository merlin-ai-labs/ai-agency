# GCP Cloud Run Environment Configuration
# Use this as a reference for GCP deployment

# ============================================
# Database Configuration - GCP Cloud SQL
# ============================================
# Option 1: Unix socket (recommended for Cloud Run with Cloud SQL)
DATABASE_URL=postgresql://ai_agency_user:PASSWORD@/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME/ai_agency

# Option 2: Public IP (if Cloud SQL has public IP)
# DATABASE_URL=postgresql://ai_agency_user:PASSWORD@PUBLIC_IP:5432/ai_agency

# Option 3: Private IP (if using VPC connector)
# DATABASE_URL=postgresql://ai_agency_user:PASSWORD@PRIVATE_IP:5432/ai_agency

# ============================================
# LLM Provider Configuration
# ============================================
LLM_PROVIDER=openai
# OPENAI_API_KEY is stored in Secret Manager, not here
# Reference: projects/PROJECT_ID/secrets/openai-api-key/versions/latest

# Alternative: Use Vertex AI
# LLM_PROVIDER=vertex
VERTEX_AI_LOCATION=us-central1

# ============================================
# GCP Configuration
# ============================================
GCP_PROJECT_ID=your-gcp-project-id
GCS_BUCKET=your-gcp-bucket-name

# ============================================
# Application Configuration
# ============================================
LOG_LEVEL=INFO
ENVIRONMENT=production

# ============================================
# Cloud Run Configuration (set via gcloud)
# ============================================
# These are typically set in the Cloud Run service config, not .env:
# --set-cloudsql-instances=PROJECT_ID:REGION:INSTANCE_NAME
# --set-secrets=OPENAI_API_KEY=openai-api-key:latest
# --min-instances=1
# --max-instances=10
# --memory=2Gi
# --cpu=2
# --timeout=300
