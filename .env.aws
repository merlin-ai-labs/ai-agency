# AWS Environment Configuration
# Use this as a reference for AWS RDS deployment

# ============================================
# Database Configuration - AWS RDS PostgreSQL
# ============================================
# Standard RDS endpoint with SSL
DATABASE_URL=postgresql://ai_agency_user:PASSWORD@mydb.abc123.us-east-1.rds.amazonaws.com:5432/ai_agency?sslmode=require

# Read replica (for read-heavy workloads)
# DATABASE_URL_READ=postgresql://ai_agency_user:PASSWORD@mydb-ro.abc123.us-east-1.rds.amazonaws.com:5432/ai_agency?sslmode=require

# Aurora PostgreSQL (compatible endpoint)
# DATABASE_URL=postgresql://ai_agency_user:PASSWORD@cluster-name.cluster-abc123.us-east-1.rds.amazonaws.com:5432/ai_agency?sslmode=require

# ============================================
# LLM Provider Configuration
# ============================================
LLM_PROVIDER=openai
# OPENAI_API_KEY should be stored in AWS Secrets Manager
# Retrieve using: aws secretsmanager get-secret-value --secret-id ai-agency/openai-key
OPENAI_API_KEY=your-openai-api-key

# ============================================
# AWS Configuration
# ============================================
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID and AWS_SECRET_ACCESS_KEY typically provided by IAM roles

# S3 Bucket for document storage (instead of GCS)
# AWS_S3_BUCKET=your-s3-bucket-name
# AWS_S3_REGION=us-east-1

# Alternative GCP for AI services
GCP_PROJECT_ID=your-gcp-project-id
GCS_BUCKET=your-bucket-name

# ============================================
# Application Configuration
# ============================================
LOG_LEVEL=INFO
ENVIRONMENT=production

# ============================================
# Security Configuration
# ============================================
# SSL/TLS Configuration
# DATABASE_SSL_CA=/path/to/rds-ca-bundle.pem
# DATABASE_SSL_MODE=require

# ============================================
# AWS App Runner / ECS Configuration
# ============================================
# These are typically set in the service configuration:
# - CPU: 1 vCPU
# - Memory: 2 GB
# - Health check: /health
# - Port: 8080
